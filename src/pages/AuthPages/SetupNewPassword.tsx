import React from "react";
import SetupNewPasswordComponent from "src/components/Auth/SetupNewPassword";

const SetupNewPasswordPage = () => {
  return (
    // <PageLayout>
    <SetupNewPasswordComponent />
    // </PageLayout>
  );
};

export default React.memo(SetupNewPasswordPage);